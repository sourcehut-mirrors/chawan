[buffer]
history = false

[external]
mime-types = "mime.types"
auto-mailcap = "mailcap"

  [[siteconf]] # test
 host = 'example\.org' # test
default-headers = { # test
	a = "b", # comment
	c = "d", # trailing comma
} # test
cookie = true # test
